[build-system]
requires = ["setuptools==80.9.0"]
build-backend = "setuptools.build_meta"

[project]
name = "vkr-rca-ml"
version = "0.1.0"
description = "Reproducible learning-to-rank research pipeline for vkr-rca"
requires-python = ">=3.12"
dependencies = [
  "numpy==2.4.4",
  "scipy==1.18.1",
  "xgboost==3.2.0",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["rca_ml*"]
